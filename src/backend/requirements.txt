# Pinned versions for reproducible builds. Bump deliberately; run tests +
# ollama smoke test after any change.
# Regenerate with `pip freeze | grep -E '^(fastapi|uvicorn|...)==' > requirements.txt`.

fastapi==0.141.1
uvicorn[standard]==0.52.1
httpx==0.28.1
jinja2==3.1.6
asyncpg==0.31.0
python-dotenv==1.2.2
PyJWT==2.13.0
requests==2.34.2
sqlalchemy==2.0.51
alembic==1.18.5
greenlet==3.5.4
posthog==7.35.5
redis==8.1.0
psycopg2-binary==2.9.12
python-multipart==0.0.32
websockets==17.0.1
cryptography==50.0.0

# Ingestion / RAG pipeline
pypdf==6.15.0
pypdfium2==5.13.0
Pillow==12.3.0
yt-dlp==2026.7.4
faster-whisper==1.2.1
numpy==2.4.6
# Unified document → Markdown conversion (docx, pptx, xlsx, epub, html, csv, json).
markitdown[docx,xlsx,pptx]==0.1.7

# Search fallback
ddgs==9.14.4

# ── Optional extras (uncomment if you want the feature) ────────────────────
# Speaker diarization for videos/podcasts. Needs a HuggingFace token in
# HF_TOKEN and one-time model-license acceptance at
# https://huggingface.co/pyannote/speaker-diarization-3.1
# pyannote.audio>=3.1
